---
import { getEntry } from 'astro:content';
import BriefLayout from '@/layouts/BriefLayout.astro';

const entry = await getEntry('briefs', 'privacy');
if (!entry) throw new Error('Missing briefs/privacy content');
---

<BriefLayout entry={entry} />
