---
import { getEntry } from 'astro:content';
import BriefLayout from '@/layouts/BriefLayout.astro';

const entry = await getEntry('briefs', 'how-it-works');
if (!entry) throw new Error('Missing briefs/how-it-works content');
---

<BriefLayout entry={entry} />
