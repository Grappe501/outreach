---
import { getEntry } from 'astro:content';
import BriefLayout from '@/layouts/BriefLayout.astro';

const entry = await getEntry('briefs', 'problem');
if (!entry) throw new Error('Missing briefs/problem content');
---

<BriefLayout entry={entry} />
