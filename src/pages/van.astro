---
import { getEntry } from 'astro:content';
import BriefLayout from '@/layouts/BriefLayout.astro';

const entry = await getEntry('briefs', 'van');
if (!entry) throw new Error('Missing briefs/van content');
---

<BriefLayout entry={entry} />
